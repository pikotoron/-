  ## サンプルです。
練習1
```
#include <stdio.h>

int main(void)
{
  printf("%d\n",? / ?);
  return 0;
}
```

練習2
```
#include <stdio.h>

int main(void)
{
  int a,b; //今回変数名は何でもいいです。
  scanf("%d %d", ? , ? );
  printf("%d ? ",a ? b); //足し算の結果を表示
  printf("%d ? ",a ? b); //引き算の結果を表示
  printf("%d ? ",a ? b); //掛け算の結果を表示
  printf("%d ? ",a ? b); //割り算の結果を表示
  printf("%d ? ",a ? b); //余りを表示
  
  return 0;
}
```

練習3
```
#include <stdio.h>
int main(void)
{
  int x; //立方体の一辺の長さと置いています。
  scanf("%d", ? );
  printf("%d\n",     ?     );
  
  return 0;
}
```

練習4
```
#include <stdio.h>
int main(void)
{
  int x,y; //縦の辺と横の辺の長さと置いています。
  scanf("   ?   ",&x,&y);
  printf("%d\n",   ?   );
  
  return 0;
}
